apiVersion: v1
kind: Service
metadata:
  name: wil42-service
spec:
  selector:
    app: wil42
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9999
      # nodePort: 31000